name: pokecomparer_trivia
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Lógica del proyecto
  http: ^1.3.0
  fl_chart: ^0.70.2

  # Base de datos local
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path_provider: ^2.0.14

  # Firebase (opcional por ahora)
  firebase_core: ^3.13.0
  firebase_auth: ^5.5.2
  cloud_firestore: ^5.6.6

  # Estilo
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true

  assets:
    - assets/DefaultBall.png
    - assets/International_Pokemon.png
    - assets/app_icon/super.png
flutter_icons:
  android: true
  ios: true
  image_path: "assets/app_icon/super.png"
